# Network
RPC_URL=https://rpc.helius.xyz/?api-key=YOUR_HELIUS_KEY
NETWORK=devnet

# Wallet
WALLET_PRIVATE_KEY_BASE58=    # Exported Phantom private key (base58) — DO NOT commit
WALLET_ADDRESS=

# Trading
MIN_PROFIT_PCT=0.5            # 0.5% = minimum net profit threshold
TRADE_AMOUNT_USD=10           # starting trade size in USD equivalent
SLIPPAGE_BPS=50               # 0.5% default
POLL_INTERVAL_MS=500          # 500ms polling (tune for RPC limits)

# Jupiter API
JUPITER_QUOTE_API=https://quote-api.jup.ag/v6/quote
JUPITER_SWAP_API=https://quote-api.jup.ag/v6/swap

# Alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Copy Trading
COPY_ENABLED=false                # Set to 'true' to enable copy trading
COPY_WALLETS=                     # Comma-separated wallet addresses to copy
COPY_BALANCE_PCT=50               # Percentage of balance to use per copy trade
COPY_MAX_SOL=0.1                  # Maximum SOL per copy trade
COPY_MIN_SOL=0.05                 # Minimum SOL value to trigger a copy
COPY_POLL_INTERVAL_MS=3000        # How often to poll wallets (ms)
COPY_SELLS=false                  # Don't copy sells (we auto-sell instead)

# Position Management (Auto-Sell Strategy)
MAX_POSITIONS=3                   # Maximum concurrent positions
TAKE_PROFIT_PCT=50                # Sell when profit reaches +50%
STOP_LOSS_PCT=-30                 # Sell when loss reaches -30%
TIME_LIMIT_MINUTES=60             # Sell after 60 minutes no matter what
TRAILING_STOP_PCT=20              # 20% trailing stop from highest value

# Ops
LOG_LEVEL=INFO
